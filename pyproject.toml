[project]
name = "kasctl"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
  "docker",
  "pyyaml",
  "questionary",
  "rich",
  "tomli-w",
]

[tool.flake8]
ignore = ["E203", "W503"]

[tool.ruff]
line-length = 120

[tool.ruff.lint]
select = ["E", "F", "I", "UP"]
